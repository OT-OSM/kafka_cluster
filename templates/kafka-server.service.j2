[Unit]
Description=Kafka
After=network.target

[Service]
User=kafka
ExecStart={{ installation_dir }}/kafka/bin/kafka-server-start.sh {{ installation_dir }}/kafka/config/server.properties
Restart=on-failure

[Install]
WantedBy=multi-user.target
